{
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db-generate": "cd ./apps/kang-server && npx drizzle-kit generate",
    "db-migrate": "cd ./apps/kang-server && npx drizzle-kit migrate"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "turbo": "^2.3.3"
  }
}
