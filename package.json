{
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "client-dev": "cd apps/client && npm run dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
